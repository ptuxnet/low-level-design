package CreationalPatterns.Factory.SimpleFactory.NotificationExample.Problem;

// Notification interface
public interface Notification {
    void send(String message);
}

// So far, so good. Not crimes yet.